<table class="table is-bordered is-fullwidth is-hoverable ">
    <thead>
        <tr>
            <th>Event: <%= person.event %></th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <th>Registered Students</th>
        </tr>
        <% users.forEach( function(user) { %>
    
            <tr><td><%= user.username %></td></tr>
        
        <% }); %>
    </tbody>

</table>